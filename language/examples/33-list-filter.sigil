⟦ Test: Filter Even Numbers
   Expected output: 3
   Count even numbers in list
⟧

λ is_even(n: ℤ) → 𝔹 = n % 2 = 0

λ count_evens(xs: [ℤ]) → ℤ ≡xs {
  [] → 0 |
  [x, .rest] → ≡(is_even(x)) {
    ⊤ → 1 + count_evens(rest) |
    ⊥ → count_evens(rest)
  }
}

λ main() → ℤ = count_evens([1, 2, 3, 4, 5, 6])
