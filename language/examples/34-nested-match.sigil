⟦ Test: Nested Pattern Matching
   Expected output: 3
   Match nested list structure
⟧

λ first_of_first(xss: [[ℤ]]) → ℤ ≡xss {
  [[x, ..], ..] → x |
  _ → 0
}

λ main() → ℤ = first_of_first([[3, 4], [5, 6]])
