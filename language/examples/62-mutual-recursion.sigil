⟦ Test: Mutually Recursive Functions
   Expected output: 1 (true)
   Even/odd using mutual recursion
⟧

λ is_even(n: ℤ) → 𝔹 ≡n {
  0 → ⊤ |
  _ → is_odd(n - 1)
}

λ is_odd(n: ℤ) → 𝔹 ≡n {
  0 → ⊥ |
  _ → is_even(n - 1)
}

λ main() → ℤ ≡(is_even(4)) {
  ⊤ → 1 |
  ⊥ → 0
}
