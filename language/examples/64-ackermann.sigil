⟦ Test: Ackermann Function
   Expected output: 7
   Ackermann(2, 2) = 7
⟧

λ ack(m: ℤ, n: ℤ) → ℤ ≡(m = 0) {
  ⊤ → n + 1 |
  ⊥ → ≡(n = 0) {
    ⊤ → ack(m - 1, 1) |
    ⊥ → ack(m - 1, ack(m, n - 1))
  }
}

λ main() → ℤ = ack(2, 2)
