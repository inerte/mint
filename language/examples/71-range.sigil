⟦ Test: Generate Range
   Expected output: 15
   Sum of range [1..5]
⟧

λ range(start: ℤ, end: ℤ) → [ℤ] ≡(start > end) {
  ⊤ → [] |
  ⊥ → [start, ..range(start + 1, end)]
}

λ sum(xs: [ℤ]) → ℤ ≡xs {
  [] → 0 |
  [x, .rest] → x + sum(rest)
}

λ main() → ℤ = sum(range(1, 5))
