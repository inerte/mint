⟦
  Sigil Standard Library - URL

  Canonical URL parsing for absolute and relative links.
  Runtime behavior is provided by compiler intrinsics for stdlib⋅url.
⟧

t Url={
  fragment:𝕊,
  host:𝕊,
  path:𝕊,
  port:Option[ℤ],
  protocol:𝕊,
  query:{𝕊↦𝕊},
  query_string:𝕊
}

t UrlError={message:𝕊}

i core⋅map

λget_query(key:𝕊,url:Url)→Option[𝕊]=core⋅map.get(key,url.query)

λhas_query(key:𝕊,url:Url)→𝔹=core⋅map.has(key,url.query)

λis_absolute(url:Url)→𝔹=#url.protocol>0

λis_anchor(url:Url)→𝔹=(#url.path=0) and (#url.fragment>0)

λparse(input:𝕊)→Result[Url,UrlError]=Err({message:"url parse intrinsic unavailable"})

λsuffix(url:Url)→𝕊=url.query_string++url.fragment
