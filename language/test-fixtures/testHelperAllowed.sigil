λmain()=>String=processA(50)++" and "++processB(-5)

λprocessA(x:Int)=>String match §numeric.isPositive(x){
  true=>"positive"|
  false=>"negative"
}

λprocessB(y:Int)=>String match §numeric.isPositive(y){
  true=>"yes"|
  false=>"no"
}
