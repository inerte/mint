⟦ TEST: Typed FFI with list return type ⟧

e fs⋅promises : { readdir : λ(String) → [String] }

λcountFiles(dir:String)→Int={
  l files=(fs⋅promises.readdir(dir):[String]);
  #files  ⟦ Works because files is [String], not Any ⟧
}

λmain()→Unit=()
