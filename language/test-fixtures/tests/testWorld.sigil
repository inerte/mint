λmain()=>Unit=()

test "test worlds capture logs" =>!Log world {
  c log=(†log.capture():†log.LogEntry)
} {
  l _=(§io.println("temporary"):Unit);
  ※check::log.contains("temporary")
}

test "test worlds start fresh" =>!Log world {
  c log=(†log.capture():†log.LogEntry)
} {
  ¬※check::log.contains("temporary")
}
