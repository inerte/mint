t ExtendedGcd={gcd:ℤ,x:ℤ,y:ℤ}

λextended_gcd(a:ℤ,b:ℤ)→ExtendedGcd match b=0{
  true→match a<0{
    true→ExtendedGcd{gcd:-a,x:-1,y:0}|
    false→ExtendedGcd{gcd:a,x:1,y:0}
  }|
  false→{
    l recursive_result=(extended_gcd(b,a%b):ExtendedGcd);
    l quotient=(((a-(a%b))/b):ℤ);
    ExtendedGcd{
      gcd:recursive_result.gcd,
      x:recursive_result.y,
      y:recursive_result.x-(quotient*recursive_result.y)
    }
  }
}
