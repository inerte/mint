i stdlib⋅string

λlevenshtein(left:𝕊,right:𝕊)→ℤ=levenshtein_from(left,0,right,0)

λlevenshtein_from(left:𝕊,left_idx:ℤ,right:𝕊,right_idx:ℤ)→ℤ match left_idx=#left{
  true→#right-right_idx|
  false→match right_idx=#right{
    true→#left-left_idx|
    false→match stdlib⋅string.char_at(left_idx,left)=stdlib⋅string.char_at(right_idx,right){
      true→levenshtein_from(left,left_idx+1,right,right_idx+1)|
      false→1+min3(
        levenshtein_from(left,left_idx+1,right,right_idx),
        levenshtein_from(left,left_idx,right,right_idx+1),
        levenshtein_from(left,left_idx+1,right,right_idx+1)
      )
    }
  }
}

λmin2(a:ℤ,b:ℤ)→ℤ match a≤b{
  true→a|
  false→b
}

λmin3(a:ℤ,b:ℤ,third:ℤ)→ℤ=min2(min2(a,b),third)
