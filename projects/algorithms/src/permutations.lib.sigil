t Selection={chosen:Int,rest:[Int]}

λpermutations(xs:[Int])=>[[Int]] match xs{
  []=>[[]]|
  _=>permutationsFromSelections(selections(xs))
}

λpermutationsFromSelections(choices:[Selection])=>[[Int]]=§list.flatMap(λ(choice:Selection)=>[[Int]]=prependAll(choice.chosen,permutations(choice.rest)),choices)

λprependAll(item:Int,xss:[[Int]])=>[[Int]]=xss map (λ(head:[Int])=>[Int]=[item]⧺head)

λprependChoice(choices:[Selection],prefix:Int)=>[Selection]=choices map (λ(choice:Selection)=>Selection={chosen:choice.chosen,rest:[prefix]⧺choice.rest})

λselections(xs:[Int])=>[Selection] match xs{
  []=>[]|
  [head,.tail]=>[{chosen:head,rest:tail}]⧺prependChoice(selections(tail),head)
}
