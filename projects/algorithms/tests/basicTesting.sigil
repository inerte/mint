λmain()=>Unit=()

test "log capture records side effects" =>!Log world {
  c log=(†log.capture():†log.LogEntry)
} {
  l _=(§io.println("captured"):Unit);
  ※check::log.contains("captured")
}

test "numeric predicates are available in projects" {
  §numeric.isEven(2)=true and §numeric.isOdd(3)=true
}

test "pure boolean passes" {
  1+1=2
}
