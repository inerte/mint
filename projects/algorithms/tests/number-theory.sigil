i src⋅extended-gcd
i src⋅modular-exponentiation
i src⋅sieve-of-eratosthenes

λmain()→𝕌=()

test "extended gcd returns gcd 2 for 240 and 46" {
  (src⋅extended-gcd.extended_gcd(240,46)).gcd=2
}

test "extended gcd satisfies bezout identity" {
  l result=(src⋅extended-gcd.extended_gcd(240,46):src⋅extended-gcd.ExtendedGcd);
  ((240*result.x)+(46*result.y))=result.gcd
}

test "sieve of 1 is empty" {
  src⋅sieve-of-eratosthenes.sieve(1)=[]
}

test "sieve of 10 finds first primes" {
  src⋅sieve-of-eratosthenes.sieve(10)=[2,3,5,7]
}

test "mod pow computes repeated squaring result" {
  src⋅modular-exponentiation.mod_pow(2,10,1000)=24
}

test "mod pow zero exponent returns one modulo modulus" {
  src⋅modular-exponentiation.mod_pow(9,0,7)=1
}

test "mod pow modulus one returns zero" {
  src⋅modular-exponentiation.mod_pow(8,5,1)=0
}
