i src⋅levenshtein-distance
i src⋅linear-search

λmain()→𝕌=()

test "linear search finds first occurrence" {
  src⋅linear-search.linear_search(2,[5,2,3,2])=1
}

test "linear search missing value returns minus one" {
  src⋅linear-search.linear_search(9,[1,2,3])=-1
}

test "levenshtein identical strings is zero" {
  src⋅levenshtein-distance.levenshtein("sigil","sigil")=0
}

test "levenshtein kitten sitting is three" {
  src⋅levenshtein-distance.levenshtein("kitten","sitting")=3
}

test "levenshtein is symmetric" {
  src⋅levenshtein-distance.levenshtein("flaw","lawn")=src⋅levenshtein-distance.levenshtein("lawn","flaw")
}

test "levenshtein handles empty strings" {
  src⋅levenshtein-distance.levenshtein("","abc")=3 and src⋅levenshtein-distance.levenshtein("abc","")=3
}
